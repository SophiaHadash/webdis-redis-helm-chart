apiVersion: v1
kind: Service
metadata:
  name: {{ include "rtdb_api_sorbasoft_net.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "rtdb_api_sorbasoft_net.name" . }}
    helm.sh/chart: {{ include "rtdb_api_sorbasoft_net.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "rtdb_api_sorbasoft_net.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
