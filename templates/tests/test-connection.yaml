apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rtdb_api_sorbasoft_net.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "rtdb_api_sorbasoft_net.name" . }}
    helm.sh/chart: {{ include "rtdb_api_sorbasoft_net.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rtdb_api_sorbasoft_net.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
